# Implementation plan: <task-slug>

## 기준과 전달

- PRD:
- 기준 커밋 / 작업 브랜치:
- 기존 dirty 파일 / 다른 활성 작업:
- 게시 요청: 없음 또는 실제 사용자 요청과 원격·base 범위

## AC별 검증 선택

| AC | 독립적인 기대 결과와 출처 | 기존 테스트 재사용 / 추가 | 경계와 선택 이유 | 잡으려는 버그 | 명령 또는 직접 행동 |
| --- | --- | --- | --- | --- | --- |
| AC-01 | 확정 PRD의 결과 | 재사용 경로 또는 추가 필요 | Unit / Integration / E2E / 직접 리뷰 중 적절한 것 | 현실적인 실패 | 실행할 명령 |

테스트 수를 목표로 삼지 않는다.
관련 기존 테스트로 충분하면 새 테스트를 만들지 않는다.
소유 모듈 mock·내부 호출 횟수 검증 대신 관찰 가능한 결과를 확인한다.

## 버그 수정 증거

해당할 때만 재현 입력, 같은 기대값, 수정 전 실제 실패 이유, 수정 후 결과와 관련 회귀 명령을 기록한다.
환경 문제로 실패한 것은 버그 재현 증거가 아니다.
mutation은 예상 실패 테스트와 관찰 결과, 원복을 별도로 기록한다.

## 반복 검증 환경

- 실제 Node 버전과 경로:
- required API/UI 앱 명령·URL·PID 또는 비적용 근거:
- 시작 데이터·초기화 범위·테스트 계정:
- 외부 sandbox / 허용 접근 / 없으면 비적용 이유:
- 직접 만든 프로세스·브라우저의 정리 또는 유지 요청:

## 구현 순서와 인계

변경 대상, 보존 동작, 공통 검증 명령, review-diff 경로를 적는다.
verifier가 판단하기 전 PASS를 미리 채우지 않는다.
